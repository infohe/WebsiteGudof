{
  "name": "gudof",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export -o build",
    "deploy": "next build && next export && git add build/ && git commit -m \"Deploy gh-pages\" && git subtree push --prefix build origin gh-pages"
  },
  "dependencies": {
    "@material-ui/core": "^4.11.0",
    "@material-ui/icons": "^4.9.1",
    "gh-pages": "^3.1.0",
    "next": "10.0.1",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
